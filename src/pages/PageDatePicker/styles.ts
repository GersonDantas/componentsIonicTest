import { IonDatetime, IonItem, IonPage } from '@ionic/react';
import styled from 'styled-components';

export const Container = styled(IonPage)``;
export const MyIonItem = styled(IonItem)``;
export const MyIonDateTime = styled(IonDatetime)``;
